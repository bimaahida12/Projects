@model int?
@{
    int a = Model.Value;
    ViewBag.proba = Model.Value;
}
@using (Html.BeginForm("WriteComment", "Posts", new {id = Model.Value.ToString()}, FormMethod.Post))
{
    @Html.Label("Write Comment")

    @Html.TextArea("content")

    <input value="Post" type="submit">
}
